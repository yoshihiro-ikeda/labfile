
/* Gain:offset_rate210_Gain */
#define TASK_EXECUTE_0001_COMPLETED()\
  do {\
    mcos_debug_printf(",[%u]offset_rate210_Gain_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_Gain_1);\
  } while (0)

/* Gain:offset_rate210_Gain1 */
#define TASK_EXECUTE_0002_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_Gain1_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_Gain1_1);\
  } while (0)

/* Gain:offset_rate210_Gain10 */
#define TASK_EXECUTE_0003_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_Gain10_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_Gain10_1);\
  } while (0)

/* Gain:offset_rate210_Gain2 */
#define TASK_EXECUTE_0004_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_Gain2_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_Gain2_1);\
  } while (0)

/* Gain:offset_rate210_Gain3 */
#define TASK_EXECUTE_0005_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_Gain3_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_Gain3_1);\
  } while (0)

/* Gain:offset_rate210_Gain4 */
#define TASK_EXECUTE_0006_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_Gain4_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_Gain4_1);\
  } while (0)

/* Gain:offset_rate210_Gain5 */
#define TASK_EXECUTE_0007_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_Gain5_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_Gain5_1);\
  } while (0)

/* Gain:offset_rate210_Gain6 */
#define TASK_EXECUTE_0008_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_Gain6_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_Gain6_1);\
  } while (0)

/* Gain:offset_rate210_Gain7 */
#define TASK_EXECUTE_0009_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_Gain7_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_Gain7_1);\
  } while (0)

/* Gain:offset_rate210_Gain8 */
#define TASK_EXECUTE_0010_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_Gain8_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_Gain8_1);\
  } while (0)

/* Gain:offset_rate210_Gain9 */
#define TASK_EXECUTE_0011_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_Gain9_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_Gain9_1);\
  } while (0)

/* Math:offset_rate210_MathFunction1 */
#define TASK_EXECUTE_0012_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_MathFunction1_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_MathFunction1_1);\
  } while (0)

/* Math:offset_rate210_MathFunction10 */
#define TASK_EXECUTE_0013_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_MathFunction10_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_MathFunction10_1);\
  } while (0)

/* Math:offset_rate210_MathFunction2 */
#define TASK_EXECUTE_0014_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_MathFunction2_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_MathFunction2_1);\
  } while (0)

/* Math:offset_rate210_MathFunction3 */
#define TASK_EXECUTE_0015_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_MathFunction3_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_MathFunction3_1);\
  } while (0)

/* Math:offset_rate210_MathFunction4 */
#define TASK_EXECUTE_0016_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_MathFunction4_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_MathFunction4_1);\
  } while (0)

/* Math:offset_rate210_MathFunction5 */
#define TASK_EXECUTE_0017_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_MathFunction5_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_MathFunction5_1);\
  } while (0)

/* Math:offset_rate210_MathFunction6 */
#define TASK_EXECUTE_0018_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_MathFunction6_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_MathFunction6_1);\
  } while (0)

/* Math:offset_rate210_MathFunction7 */
#define TASK_EXECUTE_0019_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_MathFunction7_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_MathFunction7_1);\
  } while (0)

/* Math:offset_rate210_MathFunction8 */
#define TASK_EXECUTE_0020_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_MathFunction8_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_MathFunction8_1);\
  } while (0)

/* Math:offset_rate210_MathFunction9 */
#define TASK_EXECUTE_0021_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_MathFunction9_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_MathFunction9_1);\
  } while (0)

/* S-Function:offset_rate210_S_Function */
#define TASK_EXECUTE_0022_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_S_Function_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_S_Function_1);\
  } while (0)

/* S-Function:offset_rate210_S_Function1 */
#define TASK_EXECUTE_0023_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_S_Function1_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_S_Function1_1);\
  } while (0)

/* S-Function:offset_rate210_S_Function2 */
#define TASK_EXECUTE_0024_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_S_Function2_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_S_Function2_1);\
  } while (0)

/* S-Function:offset_rate210_S_Function3 */
#define TASK_EXECUTE_0025_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_S_Function3_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_S_Function3_1);\
  } while (0)

/* S-Function:offset_rate210_S_Function4 */
#define TASK_EXECUTE_0026_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_S_Function4_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_S_Function4_1);\
  } while (0)

/* Sqrt:offset_rate210_Sqrt1 */
#define TASK_EXECUTE_0027_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_Sqrt1_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_Sqrt1_1);\
  } while (0)

/* Sqrt:offset_rate210_Sqrt2 */
#define TASK_EXECUTE_0028_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_Sqrt2_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_Sqrt2_1);\
  } while (0)

/* Sqrt:offset_rate210_Sqrt3 */
#define TASK_EXECUTE_0029_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_Sqrt3_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_Sqrt3_1);\
  } while (0)

/* Sqrt:offset_rate210_Sqrt4 */
#define TASK_EXECUTE_0030_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_Sqrt4_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_Sqrt4_1);\
  } while (0)

/* Sqrt:offset_rate210_Sqrt5 */
#define TASK_EXECUTE_0031_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_Sqrt5_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_Sqrt5_1);\
  } while (0)

/* Sum:offset_rate210_Sum */
#define TASK_EXECUTE_0032_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_Sum_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_Sum_1);\
  } while (0)

/* UnitDelay:offset_rate210_UnitDelay */
#define TASK_EXECUTE_0033_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_UnitDelay_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_UnitDelay_1);\
  } while (0)

/* UnitDelay:offset_rate210_UnitDelay1 */
#define TASK_EXECUTE_0034_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_UnitDelay1_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_UnitDelay1_1);\
  } while (0)

/* UnitDelay:offset_rate210_UnitDelay2 */
#define TASK_EXECUTE_0035_COMPLETED()\
  do {\
    mcos_debug_printf("[%u]offset_rate210_UnitDelay2_1:%.7f\n", mcos__cycle_step, (double)offset_rate210_UnitDelay2_1);\
  } while (0)
